// written by Joseph

import("dshell");
import("konoha.json");

const DCaseContext = Json;

boolean RecognitionGW(DCaseContext ctx) {
	boolean ret = true;

//check whether default gateway is recognized or not
	int gateway = $(netstat -r).search("UG");
	if (gateway == -1) {
		System.p("SystemFault:Network is disconnected.");
		return false;
	};
	return ret;
}

void main() {
	DCaseContext ctx = new DCaseContext();
	System.p(RecognitionGW(ctx));
}

main();

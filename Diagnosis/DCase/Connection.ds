// written by Joseph

import("dshell");
import("konoha.json");

const DCaseContext = Json;

boolean Connection(DCaseContext ctx) {
	boolean ret = true;

//check whether network cable is connected or not = check whether NIC is recognized or not
	int gateway = $(netstat -rn).search("eth");
	if (gateway == -1) {
		System.p("SystemFault:Network is disconnected.");
		return false;
	};
	return ret;
}

void main() {
	DCaseContext ctx = new DCaseContext();
	System.p(Connection(ctx));
}

main();

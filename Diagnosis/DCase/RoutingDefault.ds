// written by Joseph

import("Syntax.ShellStyle");
import("Type.Json");

boolean RoutingDefault(Json ctx) {
	boolean ret = false;

	String Rtable = $(route -n | grep "UG");
	if (Rtable.search("UG") > -1) {
//		System.p("Success:The gateway is registered.");
		ret = true;
	}

	return ret;
}

void main() {
	Json ctx = new Json();
	System.p(RoutingDefault(ctx));
}

main();

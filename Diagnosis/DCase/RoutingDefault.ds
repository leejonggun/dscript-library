// written by Joseph

import("dshell");
import("konoha.json");

const DCaseContext = Json;

boolean RoutingDefault(DCaseContext ctx) {
	boolean ret = true;

	String Rtable = $(netstat -rn | grep "UG");
	if (Rtable.search("UG") > 0) {
		System.p("Success:The gateway is registered. Check its routing settings.");
		ret = true;
	}

	return ret;
}

void main() {
	DCaseContext ctx = new DCaseContext();
	System.p(RoutingDefault(ctx));
}

main();
